<h2>Single User Tweet</h2>

<%= form_for @tweet do |f| %>
  <div>
    <%= f.text_field :content %> <%= f.submit "Tweet" %>
  </div>
<% end %>


<div>
<% @tweets.each do |tweet| %>
  <p></p><%= tweet.content %></p>
<% end %>
</div>